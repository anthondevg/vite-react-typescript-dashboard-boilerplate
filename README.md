# React + TypeScript + Vite + ShadCDN + React Router DOM

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Boilerplate by @anthondevg

to run

```js

pnpm install

pnpm run dev

```
